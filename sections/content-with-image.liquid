<section class="content-image">
  <div class="container">
    <div class="content-image-main {% if section.settings.layout == 'image_left' %}reverse{% endif %}">
      <div class="content-image-left">
        <h2>{{ section.settings.heading }}</h2>
        {% if section.settings.description != blank %}
          <p>{{ section.settings.description }}</p>
        {% endif %}
        {% if section.settings.button_text != blank %}
          <a href="{{ section.settings.button_link }}" class="button">{{ section.settings.button_text }}</a>
        {% endif %}
      </div>

      <div class="content-image-right">
        {% if section.settings.image %}
          <img src="{{ section.settings.image | img_url: 'medium' }}" alt="{{ section.settings.heading }}">
        {% endif %}
      </div>
    </div>
  </div>
</section>

<style>
  .content-image {
    padding: 40px 0;
  }

  .content-image-main {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: var(--color-secondary);
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 1px 1px rgba(0, 0, 0, 0.1);
    gap: 30px;
  }

  /* Reverse layout when image is on the left */
  .content-image-main.reverse {
    flex-direction: row-reverse;
  }

  .content-image-left {
    flex: 1;
    padding: 40px;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    gap: 25px;
  }

  .content-image-left h2 {
    font-size: 2rem;
    line-height: 1.3;
  }

  .content-image-left p {
    font-size: 1rem;
    line-height: 1.6;
  }

  .content-image-right {
    flex-shrink: 0;
    width: 50%;
  }

  /* When layout is image_left → image width = 30% */
  .content-image-main.reverse .content-image-right {
    width: 30%;
  }

  .content-image-right img {
    width: 100%;
    height: auto;
    object-fit: cover;
    display: block;
  }

  /* 🔹 Responsive Design */
  @media (max-width: 992px) {
    .content-image-main {
      flex-direction: column;
      gap: 20px;
      text-align: left;
    }

    /* Keep text first (left) and image second on mobile */
    .content-image-main.reverse {
      flex-direction: column;
    }

    .content-image-left {
      order: 1;
      width: 100%;
      padding: 20px;
      align-items: flex-start;
      text-align: left;
    }

    .content-image-right {
      order: 2;
      width: 100%;
    }

    .content-image-left h2 {
      font-size: 1.6rem;
    }

    .content-image-left p {
      font-size: 0.95rem;
    }
  }

  @media (max-width: 576px) {
    .content-image-left {
      padding: 15px;
    }
  }
</style>

{% schema %}
{
  "name": "Content with Image",
  "settings": [
    {
      "type": "select",
      "id": "layout",
      "label": "Layout",
      "options": [
        { "value": "image_right", "label": "Image on Right (50% width)" },
        { "value": "image_left", "label": "Image on Left (30% width)" }
      ],
      "default": "image_right"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Featured Products"
    },
    {
      "type": "text",
      "id": "description",
      "label": "Description",
      "default": "Shop the latest products"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link",
      "default": "/collections/all"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop Now"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    }
  ],
  "presets": [
    {
      "name": "Content with Image",
      "category": "Image"
    }
  ]
}
{% endschema %}
